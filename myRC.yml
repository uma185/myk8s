apiVersion: v1
kind: Replication Controller 
metadata:
  name: myonlineapp-rc
spec:
  replicas:
  selector:
    app: myonlineapp
    version: 2.6.2
  template: 
    metadata:
    labels:
        app: myonlineapp
        version: 2.6.2
    spec:
        containers:
        - name: myonlineapp-container
          image: umagurram/javaproject28feb
          ports:
          - containerPort: 8080